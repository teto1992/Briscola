/** \file bris.c
       \author Stefano Forti
     Si dichiara che il contenuto di questo file e' in ogni sua parte opera
     originale dell' autore.  */
int pickCard( char* a, int len );

int joinMatch ( int fd, int turno, char* u);
